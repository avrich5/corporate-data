# corporate-data pipeline — Python dependencies
# Generated: 2026-03-11
# Python: 3.9+
# Install: pip install -r requirements.txt

# ── Stage 01 — Structural Extractor ──────────────────────────────────────────
pandas==2.2.3
openpyxl==3.1.5
jsonschema==4.23.0

# ── Stage 02 — Semantic Analyzer (LLM) ───────────────────────────────────────
anthropic==0.49.0
openai==1.68.2
jinja2==3.1.6
aiohttp==3.11.18      # 3.11.14 was yanked (regression #10617); 3.11.18 is stable

# ── Stage 04 — Report Writer ──────────────────────────────────────────────────
python-docx==1.1.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.23.8
python-dotenv==1.2.2

# ── Stage 05 — Query Agent ───────────────────────────────────────────
# Vector store: faiss-cpu (replaces chromadb — 1.x API incompatible with NumPy 2.x)
faiss-cpu==1.13.2
numpy==1.26.4              # pinned: NumPy 2.x breaks onnxruntime + older embedding libs
sentence-transformers==2.7.0
streamlit==1.55.0
plotly==6.6.0
matplotlib==3.10.8
seaborn==0.13.2
RestrictedPython==7.4
